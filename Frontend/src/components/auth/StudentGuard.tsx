'use client'

/**
 * StudentGuard — authentication removed.
 * All pages are now publicly accessible; this component simply renders children.
 */
export const StudentGuard = ({ children }: { children: any }) => {
  return children
}
